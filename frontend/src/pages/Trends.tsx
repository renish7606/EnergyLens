import {Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import {Panel,State} from '../components/Panel';import {useTrends} from '../api/hooks';import {PageHead,useSelectedRoom} from './Shared';
export default function Trends(){const {roomId,selector}=useSelectedRoom(),r=useTrends(roomId);return <><PageHead title="Trends">{selector}</PageHead><Panel title="Daily usage and 7-day rolling average"><State loading={r.isLoading} error={r.error} empty={!r.data?.length}><div className="h-[420px]"><ResponsiveContainer><LineChart data={r.data}><XAxis dataKey="timestamp"/><YAxis/><Tooltip/><Line dataKey="kwh" name="Daily kWh" stroke="var(--accent-flow)" dot={false}/><Line dataKey="rolling_7d" name="7-day average" stroke="var(--accent-primary)" dot={false}/></LineChart></ResponsiveContainer></div></State></Panel></>}

import {Bar,BarChart,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';import {Panel,State} from '../components/Panel';import {RoomCreate} from '../components/RoomCreate';import {useRoomComparison} from '../api/hooks';import {PageHead} from './Shared';
export default function Rooms(){const r=useRoomComparison();return <><PageHead title="Room comparison"><RoomCreate/></PageHead><Panel title="Consumption per occupant"><State loading={r.isLoading} error={r.error} empty={!r.data?.length}><div className="h-72"><ResponsiveContainer><BarChart data={r.data}><XAxis dataKey="room"/><YAxis/><Tooltip/><Bar dataKey="kwh_per_occupant" fill="var(--accent-flow)"/></BarChart></ResponsiveContainer></div><div className="mt-5 overflow-auto"><table className="w-full text-left text-sm"><thead className="border-b border-line text-muted"><tr><th className="p-2">Room</th><th>Total kWh</th><th>kWh / occupant</th></tr></thead><tbody>{r.data?.map(x=><tr className="border-b border-line/60" key={x.room}><td className="p-2">{x.room}</td><td className="mono">{x.total_kwh.toFixed(2)}</td><td className="mono text-flow">{x.kwh_per_occupant.toFixed(2)}</td></tr>)}</tbody></table></div></State></Panel></>}

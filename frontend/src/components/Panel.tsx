import type {ReactNode} from 'react'; export function Panel({title,children,action}:{title:string;children:ReactNode;action?:ReactNode}){return <section className="border border-line bg-surface p-5"><div className="mb-4 flex items-center justify-between gap-3"><h2 className="text-base font-semibold">{title}</h2>{action}</div>{children}</section>}; export function State({loading,error,children,empty}:{loading?:boolean;error?:unknown;children:ReactNode;empty?:boolean}){if(loading)return <p className="py-12 text-center text-muted">Reading meter data…</p>;if(error)return <p className="py-12 text-center text-alert">Couldn’t read this meter. Check that the API is running.</p>;if(empty)return <p className="py-12 text-center text-muted">No readings for this meter yet.</p>;return <>{children}</>}
